extend type Query {
  teams(page: Int!, size: Int!): TeamPage!
  team(id: Long!): Team
}

extend type Mutation {
  createTeam(input: CreateTeamInput!): Team
  updateTeam(input: UpdateTeamInput!): Team
  deleteTeam(input: DeleteTeamInput!): Boolean

  addTeamMember(input: AddTeamMemberInput!): TeamMembership
  removeTeamMember(input: RemoveTeamMemberInput!): TeamMembership
}

type Team {
  id: Long!
  name: String!
  createdDatetime: DateTime!
  members(page: Int!, size: Int!): MemberPage!
}

type TeamPage {
  teams: [Team!]!
  pageInfo: PageInfo!
}

type TeamMembership {
  team: Team
  member: Member
}

input CreateTeamInput {
  name: String!
}

input UpdateTeamInput {
  id: Long!
  name: String!
}

input DeleteTeamInput {
  id: Long!
}

input AddTeamMemberInput {
  teamId: Long!
  memberId: Long!
}

input RemoveTeamMemberInput {
  teamId: Long!
  memberId: Long!
}
